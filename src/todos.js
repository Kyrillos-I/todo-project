function createToDo(title, description, dueDate, priority, checked){
    return {
        title,
        description,
        dueDate,
        priority,
        checked
    }
}
export default createToDo;